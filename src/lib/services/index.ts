import { getData, getPaginatedData, getDetails, postData, updateData, deleteData, patchData, signInData } from "./baseServices";

export { getData, getPaginatedData, getDetails, postData, updateData, deleteData, patchData, signInData };
